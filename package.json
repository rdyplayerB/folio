{
  "name": "folio",
  "private": true,
  "description": "Folio \u2014 an open engine and portable .folio format for NES-era graphic adventures.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node packages/zmachine/test/smoke.js && node packages/format/test/roundtrip.js && node packages/validator/test/catches.js && node packages/validator/test/graph.js && node packages/validator/test/design.js && node conformance/parity.js"
  },
  "bin": {
    "folio": "packages/cli/folio.js"
  }
}