#  Screenshots in here are verification output, not build product. They get
#  regenerated by the tools that check the site and the player, so they are
#  worth looking at once and not worth carrying in the history.
*.png
*.folio
